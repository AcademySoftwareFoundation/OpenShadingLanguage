shader
test_vector_v_dfloat(output color Cout = 0)
{
    float val = 0.5*(v);
    float val2 = u;
     
              
    vector p1 = vector(val);
    if (int(P[0]*64)%2==0) {
        color p2 = vector(val2);
        p1 = p2;
    }
            
    vector dxc = Dx(p1);
    vector dyc = Dy(p1);
    Cout = dxc + dyc;
    
}
