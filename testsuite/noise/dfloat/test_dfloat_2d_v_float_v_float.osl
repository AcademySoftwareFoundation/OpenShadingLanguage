shader
test_dfloat_2d_v_float_v_float (string type = "perlin", output color Cout = 0)
{
   float val = noise (type, u, v);    
    val *= 2.0;
    Cout = val;
    Cout[1] = Dy(val);    
    Cout[2] = Dx(val);    
    
    if (int(P[0]*64)%2 == 0) {
        float val2 = noise (type, v, u);
        Cout[1] += Dy(val2) + Dx(val2);
    }
}
