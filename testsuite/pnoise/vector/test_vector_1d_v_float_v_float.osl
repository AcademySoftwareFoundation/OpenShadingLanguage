shader
test_vector_1d_v_float_v_float (string type = "perlin", output color Cout = 0)
{
    vector val = pnoise (type, u, 64*v);    
    val *= 2.0;
    Cout = val;    
    
    if (int(P[0]*64)%2 == 0) {
        vector val2 = pnoise (type, v, 64*u);
        val2 *= 1.125;
        Cout = val2;
    }
}
