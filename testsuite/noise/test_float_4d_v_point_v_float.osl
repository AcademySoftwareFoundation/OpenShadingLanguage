shader
test_float_4d_v_point_v_float (string type = "perlin", output color Cout = 0)
{
    float val = noise (type, point(u, v, u - v), u);
    val *= 2.0;
    Cout = val;
    if (int(P[0]*64)%2 == 0) {
        Cout[1] = noise (type, point(v, u, v - u), v);
    }
}
