shader
test_v_float_mul_u_float (output color Cout = 0)
{
    float factor = 1.0/(2*raytype("camera"));
              
    float numerator = u;
    float red = 0.0;
    float green = 0.5;
    float blue = 1.0;
    red = numerator*factor;
    green = numerator*int(v);
    blue = v*factor;
    
    Cout = color(red,green,blue);
}
