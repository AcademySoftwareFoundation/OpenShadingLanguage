shader
test_dfloat_1d_v_float (string type = "perlin", output color Cout = 0)
{
   float val = noise (type, u);    
    val *= 2.0;
    Cout = val;
    Cout[2] = Dx(val);    
    
    if (int(P[0]*64)%2 == 0) {
        float val2 = noise (type, v);
        Cout[1] += Dy(val2);
    }
}
