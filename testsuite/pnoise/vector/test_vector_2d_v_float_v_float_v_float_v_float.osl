shader
test_vector_2d_v_float_v_float_v_float_v_float (string type = "perlin", output color Cout = 0)
{
    float val = pnoise (type, u, v, 32*u, 32*v);
    val *= 2.0;
    Cout = val;
    if (int(P[0]*64)%2 == 0) {
        vector val2 = pnoise (type, v, u, 64*v, 64*u);
        val2 *= 1.125;
        Cout = val2;
    }
}
