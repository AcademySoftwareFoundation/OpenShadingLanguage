shader
test_dfloat_1d_v_float_v_float (string type = "perlin", output color Cout = 0)
{
    float val = pnoise (type, u, 64*v);    
    val *= 2.0;
    Cout = val;
        
    Cout[2] = Dx(val);        
    if (int(P[0]*64)%2 == 0) {
        float val2 = pnoise (type, v, 64*u);
        Cout[1] += Dy(val2);        
    }
}
