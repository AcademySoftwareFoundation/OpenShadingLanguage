shader
test_vector_3d_v_point (string type = "perlin", output color Cout = 0)
{
    color val = noise (type, point(u, v, u - v));
    val *= 2.0;
    Cout = val;
    if (int(P[0]*64)%2 == 0) {
        Cout = noise (type, point(v, u, v - u));
    }
}
