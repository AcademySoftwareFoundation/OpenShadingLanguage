shader
test_abs_v_dfloat (output color Cout = 0)
{
    float  val = -P[0];
    float  val2 = P[1];
    
    float d = abs(val);
    if (int(P[0]*64)%2==0) {
        d = abs(val2);
    }
    
    Cout = color(d, Dx(d), Dy(d));
}
