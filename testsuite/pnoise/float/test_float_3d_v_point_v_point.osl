shader
test_float_3d_v_point_v_point (string type = "perlin", output color Cout = 0)
{
    
    float val = pnoise (type, point(u, v, u - v), point(32*u, 64*v, 16*(u+v)));
    val *= 2.0;
    Cout = val;
    if (int(P[0]*64)%2 == 0) {
        Cout[1] = pnoise (type, point(v, u, v - u), point(32*v, 64*u, 48*(u+v)));
    }
}
