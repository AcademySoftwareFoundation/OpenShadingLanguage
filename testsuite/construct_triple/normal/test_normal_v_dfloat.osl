shader
test_normal_v_dfloat(output color Cout = 0)
{
    float val = 0.5*(v);
    float val2 = u;
     
              
    normal p1 = normal(val);
    if (int(P[0]*64)%2==0) {
        color p2 = normal(val2);
        p1 = p2;
    }
            
    normal dxc = Dx(p1);
    normal dyc = Dy(p1);
    Cout = dxc + dyc;
    
}
