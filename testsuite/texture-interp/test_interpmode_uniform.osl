shader
test_interpmode_uniform (string filename = "../common/textures/mandrill.tif",
      output color Cout = 0)
{
    Cout = (color) texture (filename, u, v, "width", 8.0,
                            "interp", "cubic");
}
