shader
test_float_1d_v_float (string type = "perlin", output color Cout = 0)
{
    float val = noise (type, u);    
    val *= 2.0;
    Cout = val;    
    
    if (int(P[0]*64)%2 == 0) {
        Cout[1] = noise (type, v);
    }
}
