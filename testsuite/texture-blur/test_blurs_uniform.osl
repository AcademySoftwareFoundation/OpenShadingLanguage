shader
test_blurs_uniform (string filename = "../common/textures/mandrill.tif",
      output color Cout = 0)
{
    float b = 0.1;
    Cout = (color) texture (filename, u, v, "blur", b, "tblur", 0.2, "sblur", 0.3);
}
