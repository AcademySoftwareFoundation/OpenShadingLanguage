shader
test_normal_u_float (output color Cout = 0)
{
    float  val = 1.0/(2*raytype("camera"));
    float val2 = val/2;

    normal p1 = normal(val);
    if (int(P[0]*64)%2==0) {
        normal p2 = normal(val2);
        p1 = p2;
    }
    
    Cout = p1;
}
