shader
test_vector_u_float (output color Cout = 0)
{
    float  val = 1.0/(2*raytype("camera"));
    float val2 = val/2;

    vector p1 = vector(val);
    if (int(P[0]*64)%2==0) {
        vector p2 = vector(val2);
        p1 = p2;
    }
    
    Cout = p1;
}
