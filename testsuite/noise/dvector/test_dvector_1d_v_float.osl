shader
test_dvector_1d_v_float (string type = "perlin", output color Cout = 0)
{
    vector val = noise (type, u);    
    val *= 2.0;
    Cout = Dx(val);    
    
    if (int(P[0]*64)%2 == 0) {
        vector val2 = noise (type, v);
        Cout += Dy(val2);
    }
}
