shader
test_vector_v_space_3xu_float (output color Cout = 0)
{
    float  val = 1.0/(2*raytype("camera"));
    float val2 = val/2;

    string vectorspaces[5] = { "common", "object", "shader", "world", "camera"};
    string vectorspace = vectorspaces[int(P[0]*64)%5];


    vector c1 = vector(vectorspace, val, val, val);
    if (int(P[0]*64)%2==0) {
        c1 = vector(vectorspace, val2, val2, val2);
    }
    
    Cout = c1;
}
