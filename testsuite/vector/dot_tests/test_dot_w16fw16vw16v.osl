shader test_dot_w16fw16vw16v (output float ddot = 0,
                              output float mddot = 0)
//testshade --batched --llvm_debug -g 4 4 test_dot_w16dfw16dvw16v | grep osl_dot
{

    vector a = vector(u+v, u-v, -u-v);
    vector b = vector(u,v,u);
    
    float ddot_ = 0.0;
    ddot_ = dot(a,b);
    
         
    float mddot_ = 0.0;
    
              
    if(int(P[0]*200)%2){
          
       
           
           mddot_ = dot(vector(P[0], u, v), vector(P[1], v, u));
}
ddot = ddot_;
mddot = mddot_;

}
