shader
test_u_float_div_v_float_masked (output color Cout = 0)
{
    float divisor = 3 + u;
                  
    float numerator = 3.0;
    float red = 0.0;
    float green = 0.5;
    float blue = 1.0;
    if (int(P[0]*64)%2==0) {    
        red = numerator/divisor;
        green = numerator/int(v);
        blue = (2*numerator)/divisor;
    }
    
    Cout = color(red,green,blue);
}
