shader
test_v_int_div_u_int (output color Cout = 0)
{
    int divisor = 2*raytype("camera");
                  
    int numerator = int(64*v);
    int red = 0;
    int green = 32;
    int blue = 64;
    red = numerator/divisor;
    green = numerator/int(v);
    blue = int(64*v)/divisor;
    
    Cout = color(red/64.0,green/64.0,blue/64.0);
    
}
