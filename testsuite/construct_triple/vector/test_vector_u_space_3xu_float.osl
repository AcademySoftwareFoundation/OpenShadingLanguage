shader
test_vector_u_space_3xu_float (string vectorspace = "--param vectorspace must_be_provided", output color Cout = 0)
{
    float  val = 1.0/(2*raytype("camera"));
    float val2 = val/2;

    vector c1 = vector(vectorspace, val, val, val);
    
    Cout = .666*c1;
    if (int(P[0]*64)%2==0) {
        Cout = vector(vectorspace, val2, val2, val2);
    }
    
}
