shader
test_u_int_div_v_int (output color Cout = 0)
{
    int divisor = 196 + int(64*u);
                  
    int numerator = 128*64;
    int red = 0;
    int green = 32;
    int blue = 64;
    red = numerator/divisor;
    green = numerator/int(v);
    blue = (2*numerator)/divisor;
    
    Cout = color(red/64.0,green/64.0,blue/64.0);
}
