shader
test_float_4d_v_point_u_float (string type = "perlin", output color Cout = 0)
{
    float  u_val1 = raytype("camera")/4.0;
    float  u_val2 = 2.0*raytype("camera")/4.0;

    float val = noise (type, point(u, v, u - v), u_val1);
    val *= 2.0;
    Cout = val;
    if (int(P[0]*64)%2 == 0) {
        Cout[1] = noise (type, point(v, u, v - u), u_val2);
    }
}
