shader
test_asin_w16f_f (output color res = 0,
               output color res_m = 0)
{
    float a = 1.0/(2*raytype("camera"));
    float b = 1.0/(3*raytype("camera"));
    float r = asin(a);
    //res = color (r);

    float fr = 0.0;
    if (int(P[0]*200)%2){

        fr = asin(b);

    }
    res_m = color(fr);
    res = color(r);
}