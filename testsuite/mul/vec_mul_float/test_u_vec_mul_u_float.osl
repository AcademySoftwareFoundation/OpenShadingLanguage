shader
test_u_vec_mul_u_float (output color Cout = 0)
{
    float u_float = raytype("camera");    
    float v_float = u;
    
    color u_color = color(0.3*u_float,0.5*u_float, 0.75*u_float);
    
    Cout = u_color*(0.25*u_float);
        
    if (int(P[0]*64)%2==0) {
        Cout = u_color*(0.75*u_float);
    }    
}
