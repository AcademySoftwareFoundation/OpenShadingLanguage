#if 1
float helper2(float val)
{
    float r = val;
    if (P[0] < 1.0/3.0) {        
        //return val;
        exit();
    }
    r = 0.5;
    return r;
}


float helper(float val)
{
    float r2 = helper2(val);
    //if (P[0] > 1.0/6.0) 
    {        
        r2 = 1.0;
    }
    return r2;
}
#endif

#if 1
shader test_vary7b(output color c = 0.0)
{
    //float in = c;
    //float out = helper2(in);
    //float out = in;
    float r = 1.0;
    int i = 0;
    for(; i < 3; ++i)
    {
        if (P[0] > 0.5) 
        {
            if (i == 10) 
            {
                r = 0.5;
                return;
                r = 0.75;
            }
            if (i == 1) 
            {
                r = 0.15;
                return;
                r = 0.25;
            }
        }
        r = 0.25;
    }
    c = color(r, float(i)/3, 0);
}
#endif

