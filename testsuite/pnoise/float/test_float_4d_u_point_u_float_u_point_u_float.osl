shader
test_float_4d_u_point_u_float_u_point_u_float (string type = "perlin", output color Cout = 0)
{
    float  u_val1 = raytype("camera")/4.0;
    float  u_val2 = 2.0*raytype("camera")/4.0;
    float  u_val3 = 32*raytype("camera");
    float  u_val4 = 64*raytype("camera");

    float val = pnoise (type, point(u_val1, u_val2, u_val1/u_val2), u_val1, point(u_val3, u_val4, u_val3 + u_val4), u_val3);
    val *= 2.0;
    Cout = val;
    if (int(P[0]*64)%2 == 0) {
        Cout[1] = pnoise (type, point(u_val2, u_val1, u_val2/u_val1), u_val2, point(u_val4, u_val3, u_val3 + u_val4), u_val4);
    }
}
