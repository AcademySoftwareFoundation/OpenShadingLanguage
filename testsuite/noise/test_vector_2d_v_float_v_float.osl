shader
test_vector_2d_v_float_v_float (string type = "perlin", output color Cout = 0)
{
    color val = noise (type, u, v);
    val *= 2.0;
    Cout = val;
    if (int(P[0]*64)%2 == 0) {
        Cout = noise (type, v, u);
    }
}
