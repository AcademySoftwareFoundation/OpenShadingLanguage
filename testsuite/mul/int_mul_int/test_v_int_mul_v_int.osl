shader
test_v_int_mul_v_int (output color Cout = 0)
{
    int factor = 64-int(64*u);
     
                  
    int numerator = 64*int(64*v);
    int red = 0;
    int green = 32;
    int blue = 64;
    red = numerator*factor;
    green = numerator*int(v);
    blue = int(64*v)*factor;
    
    Cout = color(red/4096.0,green/4096.0,blue/4096.0);    
}
