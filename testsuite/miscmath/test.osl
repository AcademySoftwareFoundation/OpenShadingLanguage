#ifndef M_E
#define M_E 2.718281828459
#endif


shader
test ()
{
    {
        float a = 101;
        printf ("  fabs (%g) = %g\n", a, fabs(a));
        a = -137;
        printf ("  fabs (%g) = %g\n", a, fabs(a));
    }

    { 
        vector a = 101;
        printf ("  fabs (%g) = %g\n", a, fabs(a));
        a = -137;
        printf ("  fabs (%g) = %g\n", a, fabs(a));
    }

    printf (" varying:\n");
    {
        float a = u;
        printf ("  fabs (%g) = %g\n", a, fabs(a));
    }

    {
        color a = u;
        printf ("  fabs (%g) = %g\n", a, fabs(a));
    }
}
