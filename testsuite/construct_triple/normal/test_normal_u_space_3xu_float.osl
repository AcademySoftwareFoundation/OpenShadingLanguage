shader
test_normal_u_space_3xu_float (string normalspace = "--param normalspace must_be_provided", output color Cout = 0)
{
    float  val = 1.0/(2*raytype("camera"));
    float val2 = val/2;

    normal c1 = normal(normalspace, val, val, val);
    
    Cout = .666*c1;
    if (int(P[0]*64)%2==0) {
        Cout = normal(normalspace, val2, val2, val2);
    }
    
}
